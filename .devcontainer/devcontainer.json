{
  "name" : "Ratio1 SDK Development Container",
  
  "dockerFile" : "Dockerfile",
  
  "runArgs": [
    //"--gpus=all", // Uncomment this line to enable GPU support
    "--hostname",
    "nepsdk"    
  ],

 "mounts": [
    "source=naeural-sdk-config,target=/root/.ratio1,type=volume"
  ],


  "customizations": {
    "vscode" : {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}